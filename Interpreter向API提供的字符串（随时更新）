Interpreter向API提供的字符串（随时更新）

“00”：创建数据库   后面直接跟数据库名
(e.g.)
“00D_1” 创建一个名为D_1的数据库

“01”：创建表  给出表名+属性名 属性名可以没有，多属性名用.分隔，最后一位表示类型
(e.g.)
“01Balance,ele1i.ele2i.ele3c” 建表Balance，包含ele1,ele2,ele3三个属性，前两个是int，第三个是char

“02”：创建索引
“10”：删除数据库
“11”：删除表
“12”：删除索引
-----------以上有些是重点-----------
“21”：有where的select
分句之间用逗号，属性之间用点号，条件之间用&或者|
(e.g.)
“ele1.ele2,Table,a<0&b>0”表示 
select ele1,ele2
from Table
where a<0 and b>0

“20”：无where的select
上文的格式中，去掉条件，别的一样